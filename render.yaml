services:
  - type: web
    name: backend-foundations-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn calculator.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ENV
        value: production
